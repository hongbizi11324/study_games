# 造梦西游复刻 · 学习复习总结（M0 → M4 全绿）

> 更新于 2026-09-05。用途：复习 + 面试准备。里程碑 M0→M4 已全部达成。

---

## 0. 里程碑完成图（全部 ✅）

```
M0 C#三件套 ✅ → M1 物理跑通 ✅ → M2 战斗闭环+刷新池 ✅
→ M3 打击感+相机+暂停 ✅ → M4 工程化（零GC实证+SO化+清理）✅
→ M5 可选扩展（BOSS 三阶段 / Spine 骨骼）
```

---

## 1. 技术栈全景（每项 = 面试可讲）

| 层 | 技术 | 落地位置 |
|---|---|---|
| 表现层 | Animator 状态机（参数驱动 + Play 直连两种模式） | Player_AC / Monster_AC |
| 表现层 | UGUI 血条（Canvas + Image.Filled + 事件驱动） | HealthBarUI.cs |
| 逻辑层 | 泛型 FSM（IState + StateMachine 三重防呆） | StateMachine.cs |
| 逻辑层 | 输入缓冲（攻击中立即连招、空闲/受击 0.2s 缓存） | Player.cs IsBusy/IsAttacking |
| 数据层 | ScriptableObject 配置（PlayerConfig / MonsterConfig） | Assets/ScriptableObjects/ |
| 数据层 | SO 事件通道（无参/带载荷/专用三形态） | GameEvent / PlayerHealthEvent / ScreenShakeEvent / MonsterDiedEvent |
| 服务层 | 泛型对象池（预热 + Stack + 池化怪物刷新） | ObjectPool.cs + MonsterSpawner.cs |
| 物理 | Rigidbody2D + 地面检测（NonAlloc 零 GC）+ 二段跳 | Player.cs |
| 打击感 | HitStop（timeScale 慢动作）+ 屏幕震动（SO 事件，暴击差异化） | HitStop.cs / ScreenShake.cs |
| 相机 | Cinemachine 2D 跟随 + Pixel Perfect（1:1 配平） | CM vcam + Main Camera |
| 工程化 | Profiler 实测 Update 零 GC + 全数值 SO 化 + 遗留清理 | 实测通过 |

---

## 2. 核心代码模式速记（面试必背）

### 泛型 FSM 三重防呆
```csharp
public bool ChangeState(TState next)
{
    if (EqualityComparer<TState>.Default.Equals(next, Current)) return false;  // 同态
    if (!states.TryGetValue(next, out var target)) return false;               // 未注册
    if (!CanTransitionTo(next)) return false;                                  // 非法转移
    // Exit(旧) → 切 Current → Enter(新)
    return true;
}
```

### SO 事件通道三形态
```csharp
// 无参（GameEvent / MonsterDiedEvent）
private event Action handlers;
// 带载荷（PlayerHealthEvent）
private event Action<int, int> handlers;
// 带载荷（ScreenShakeEvent）
private event Action<float, float> handlers;
// 统一模式：Raise/Register/Unregister + OnEnable订阅/OnDisable退订
```

### 零 GC 三件套（统一思想三处落地）
```csharp
// 1. 对象池：预热 + Stack 复用
// 2. 地面检测：OverlapCircleNonAlloc + 预分配数组
// 3. 攻击判定：OverlapBoxNonAlloc + 预分配数组
// 红线：分配移出热路径（Update 内不 new、不装箱、不拼字符串）
```

---

## 3. 关键设计决策（面试深度）

| 决策 | 为什么 | 可延伸讲 |
|---|---|---|
| 怪物 AI 用代码 FSM 而非 Animator | 决策（距离/冷却/迟滞）是数据计算，Animator 只表达表现 | 代码 FSM 管决策、Animator 管表现，两种协作模式 |
| 攻击用 Animation Event 而非计时器 | 判定时机是动画的数据，换帧不改代码 | 事件帧晚于连招取消 = 空刀（真实动作机制） |
| 迟滞区间（Detect 5 / Lose 7） | 防阈值边界抖动 | 进得去、退不出的缓冲带 |
| 输入缓冲 | 攻击中立即连招 + 空闲/受击缓存意图 | 玩家手感容错 vs 精确帧输入 |
| HitStop 用 WaitForSecondsRealtime | 不受 timeScale 影响，慢动作可恢复 | 时间缩放与真实时间的区别 |
| 相机边界用物理墙而非 Confiner | Tuanjie 裁剪版无 Confiner；物理约束白送 | 关卡设计约束 vs 相机逻辑约束 |
| Pixel Perfect 1:1 配平 | 参考分辨率=屏幕分辨率整数倍，无拉伸无警告 | 像素对齐原理 |

---

## 4. 踩坑实录（面试聊"怎么排查"的素材）

1. 缺组件 → 零报错静默失效（Wuying 没挂 Monster 脚本）
2. 接线空 → NRE 刷屏（groundCheck 没拖）
3. Body offset 2.43 → 逻辑位置与表现位置分家
4. Animation Event 字段名坑：Tuanjie 是 `functionName`（无 m_ 前缀），用 `m_FunctionName` 搜不到
5. Tuanjie 汉化坑：Animator 状态的 Tag 叫「标签」，在状态名正下方
6. Tuanjie 裁剪坑：Cinemachine 无 Confiner（2D/3D 都没有），Pixel Perfect 无 Dynamic 字体模式
7. 序列帧 Duration 0.25 → 鬼影（永远 0）
8. 字母序 `_10 < _8` → 多选拖帧乱序
9. Time.timeScale 与 HitStop/暂停打架 → Realtime 等待 + 恢复兜底
10. TMP 中文字体 → Noto Sans SC + Characters from File（Tuanjie 无 Dynamic）

---

## 5. 面试金句（每条都有实证支撑）

1. **泛型 + 零 GC**："泛型池预热分配 + Stack 复用，Profiler 实测 Player.Update/Monster.Update GC Alloc 归零；剩下的 ~0.7KB 在 PlayerLoop 根节点，是引擎自身开销——我区分了引擎开销和代码开销。"
2. **事件架构演进**："单例 EventBus 三宗罪（魔法字符串/悬空订阅/全局耦合）→ SO 事件通道 + OnEnable/OnDisable 订阅平衡，禁用订阅者事件精确跳过它。"
3. **AI 架构**："GoF State 模式泛型化 + 构造注入，转移表显式注册 = 设计文档；代码 FSM 管决策、Animator 管表现，两种协作模式（参数驱动/Play 直连）按角色选用。"
4. **打击感**："HitStop（timeScale 慢动作 + Realtime 恢复）+ SO 事件驱动的屏幕震动（暴击差异化震幅）+ 输入缓冲，三件套分别解决命中确认、反馈强度、连招容错。"
5. **数据驱动**："所有数值 ScriptableObject 承载，改数值不动代码；事件通道用 Action（快、不可序列化），Inspector 暴露才用 UnityEvent。"

---

## 6. 面试高频追问（提前想好）

- 为什么 `struct, Enum` 约束？→ 类型系统消灭魔法字符串 + 值类型不装箱
- 对象池和 `Instantiate` 的 GC 差异？→ 分配移出热路径，长寿对象晋升 gen2 后 GC 不碰
- `GetKeyDown` vs `GetKey`？→ 边沿 vs 电平
- 协程原理？→ 编译器生成状态机，yield 挂起/恢复
- `Play(hash)` vs `SetTrigger`？→ 强制打断 vs 过渡条件触发
- 为什么相机跟随用物理墙？→ 引擎裁剪没 Confiner + 物理约束白送
- 暂停和 HitStop 怎么不打架？→ Realtime 等待 + Resume 兜底 timeScale

---

## 7. M5 可选扩展

- [ ] BOSS 碧水神君三阶段（血量比例切换 FSM）
- [ ] Spine 骨骼动画（赤月神君素材已备）
- [ ] 伤害飘字 / 死亡粒子特效
- [ ] Addressables 资源分组加载
- [ ] 音效/音乐接入（Music.swf 素材）

---

*至此，从"零基础 Unity/C#"到"完整可玩 demo + 工程化实证"的旅程完成。复习节奏：先过 §1-§3 找回设计理由，再背 §4-§6（坑与话术是面试主战场），§7 是下一站。*
